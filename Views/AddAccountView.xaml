<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="trackr.Views.AddAccountView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:trackr"
    xmlns:components="clr-namespace:trackr.Components"
    Title="Add Account">

    <ContentPage.Resources>

        <local:EnumDisplayNameConverter
            x:Key="EnumDisplayNameConverter"/>

    </ContentPage.Resources>

    <Grid
        Padding="30"
        RowSpacing="20"
        RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto">

        <!-- Add Account Header -->
        <Grid
            Grid.Row="0"
            ColumnDefinitions="*,Auto">

            <!-- Add Account Title -->
            <Label
                Grid.Column="0"
                Style="{StaticResource PageTitle}"
                Text="Add Account"/>

            <!-- Cancel Button -->
            <ImageButton
                Grid.Column="1"
                BackgroundColor="{StaticResource SurfaceLight}"
                CornerRadius="20"
                HorizontalOptions="End"
                Command="{Binding CloseCommand}">

                <ImageButton.Source>

                    <FontImageSource
                        Glyph="&#xf057;"
                        Size="40"
                        Color="{AppThemeBinding 
                        Light={StaticResource PrimaryAccentPurpleLight}, 
                        Dark={StaticResource PrimaryAccentPurpleDark}}"/>

                </ImageButton.Source>

            </ImageButton>

        </Grid>

        <!-- Name -->
        <VerticalStackLayout
            Grid.Row="1"
            Spacing="5">

            <Label
                Style="{StaticResource SectionHeader}"
                Text="ACCOUNT NAME *"/>

            <Entry
                Text="{Binding AccountName}"
                Placeholder="e.g. Main Chequing"/>

        </VerticalStackLayout>

        <!-- Bank -->
        <VerticalStackLayout
            Grid.Row="2"
            Spacing="5">

            <Label
                Style="{StaticResource SectionHeader}"
                Text="BANK INSTITUTION *"/>

            <Picker
                IsEnabled="{Binding ArePickersEnabled}"
                ItemsSource="{Binding BankInstitutions}"
                ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDisplayNameConverter},ConverterParameter=Long}"
                SelectedItem="{Binding SelectedInstitution}"
                Title="Select Bank Institution"/>

        </VerticalStackLayout>

        <!-- Type -->
        <VerticalStackLayout
            Grid.Row="3"
            Spacing="5">

            <Label
                Style="{StaticResource SectionHeader}"
                Text="ACCOUNT TYPE *"/>

            <Picker
                IsEnabled="{Binding ArePickersEnabled}"
                ItemsSource="{Binding AccountTypes}"
                ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDisplayNameConverter}}"
                SelectedItem="{Binding SelectedType}"
                Title="Select Account Type"/>

        </VerticalStackLayout>

        <!-- Account Balance -->
        <VerticalStackLayout
            Grid.Row="4"
            Spacing="5">

            <Label
                Style="{StaticResource SectionHeader}"
                Text="ACCOUNT BALANCE"/>

            <Entry
                Text="{Binding CurrentBalance}"
                Keyboard="Numeric"
                Placeholder="0.00"/>

        </VerticalStackLayout>

        <!-- CSV Import Confirmation -->
        <Border
            Grid.Row="5"
            IsVisible="{Binding HasPendingImport}"
            BackgroundColor="{AppThemeBinding Light={StaticResource CategoryGreenSurfaceLight}, Dark={StaticResource CategoryGreenSurfaceDark}}">

            <VerticalStackLayout
                Spacing="10">

                <VerticalStackLayout>

                    <!-- Confirmation -->
                    <Label
                        Style="{StaticResource CardTitle}"
                        Text="CSV Imported"
                        HorizontalTextAlignment="Center"/>

                    <!-- File Name -->
                    <Label
                        Style="{StaticResource SectionHeader}"
                        Text="{Binding PendingImport.FileName}"
                        HorizontalTextAlignment="Center"/>

                </VerticalStackLayout>

                <!-- Transactions Summary -->
                <Grid
                    ColumnDefinitions="*,*,*">

                    <!-- Transactions -->
                    <VerticalStackLayout
                        Grid.Column="0">

                        <Label
                            Style="{StaticResource SummaryCardValue}"
                            Text="{Binding PendingImport.PendingBatch.ImportedCount}"
                            TextColor="{StaticResource PositiveMoney}"
                            HorizontalTextAlignment="Center"/>

                        <Label
                            Style="{StaticResource SectionHeader}"
                            Text="TRANSACTIONS"
                            HorizontalTextAlignment="Center"/>

                    </VerticalStackLayout>

                    <!-- Possible Duplicates -->
                    <VerticalStackLayout
                        Grid.Column="1">

                        <Label
                            Grid.Column="2"
                            Style="{StaticResource SummaryCardValue}"
                            Text="{Binding PendingImport.PendingBatch.PossibleDuplicateCount}"
                            TextColor="Yellow"
                            HorizontalTextAlignment="Center"/>

                        <Label
                            Style="{StaticResource SectionHeader}"
                            Text="DUPLICATES"
                            HorizontalTextAlignment="Center"/>

                    </VerticalStackLayout>

                    <!-- Skipped -->
                    <VerticalStackLayout
                        Grid.Column="2">

                        <Label
                            Grid.Column="3"
                            Style="{StaticResource SummaryCardValue}"
                            Text="{Binding PendingImport.PendingBatch.ErrorCount}"
                            TextColor="{StaticResource NegativeMoney}"
                            HorizontalTextAlignment="Center"/>

                        <Label
                            Style="{StaticResource SectionHeader}"
                            Text="SKIPPED"
                            HorizontalTextAlignment="Center"/>

                    </VerticalStackLayout>

                </Grid>

            </VerticalStackLayout>

        </Border>

        <!-- Buttons -->
        <components:ButtonGroup
            Grid.Row="6"
            PrimaryButtonCommand="{Binding AddAccountCommand}"
            SecondaryButtonCommand="{Binding ImportCSVCommand}"
            PrimaryButtonText="Add Account"
            SecondaryButtonText="Import CSV"
            IsVisible="{Binding ShowImportCSVButton}"/>

        <components:ButtonGroup
            Grid.Row="6"
            IsVisible="{Binding HasPendingImport}"
            PrimaryButtonCommand="{Binding AddAccountCommand}"
            SecondaryButtonCommand="{Binding ClearPendingImportCommand}"
            PrimaryButtonText="Add Account"
            SecondaryButtonText="Cancel CSV Import"
            IsSecondaryButtonDestructive="true"/>

    </Grid>

</ContentPage>  